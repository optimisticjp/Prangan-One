# Project memory and next steps

This file exists so a future Claude Code session (or a human) can pick this project up without re-deriving everything from the source. Read this before making architectural changes. Updated after each major session; the most recent session is summarized first.

## Project identity

**What this is:** Prangan One, "The Society OS," a Gujarati-first, multi-tenant society management platform. It started as a single-society demo for Rajhans Tower (a real 24-flat society in Surat), which remains the first real society on the platform, created from the owner console like any other, not hardcoded as the only one. Domain: pranganone.com. Support: care@pranganone.com.

**The core stance that shapes every screen:** the committee is the customer and the hero, residents are upside. A society must get full value from the committee side alone, with zero residents logged in. Resident self-service is a second adoption wave, never a dependency for the society to run.

**Current phase:** Almost everything in `docs/PRANGAN_ONE_ROADMAP.md` that can be built without a live Supabase project now exists as real, tested code: the full role model, the subscription lifecycle with a working write-guard, the complete owner backend, real (if locally-scoped) magic-link auth code, CSV import, receipt cancellation, code splitting, an error boundary, and the public marketing site. The schema is fully written for the target state. What's left is fundamentally about a live database existing: connecting it, running the roadmap's own hard checkpoint (a real second society, verified isolation), and then the two things that were always going to need it (file storage, and the membership-claim flow that only makes sense once real auth users exist).

**Who it's for:**
- **Residents:** check their bill, pay, complain, read notices, vote, RSVP to events.
- **Committee (admin):** the treasurer/secretary/president running the society day to day. Almost never a software person, see "Designing for the committee's actual skill level" below.
- **Accountant:** a narrower role focused on the money side and audit exports.
- **Owner:** creates and manages every society on the platform from `/owner/*`. This role can be more technical than the others, since it's the platform owner's own tool, not a committee-facing one.

*(Note on reading the session log below: earlier sessions correctly used "Essancia" when describing who was operating the platform at the time that was written. That's accurate history, left as-is rather than sanitized, the standing description above is what's current now.)*

## Session 11 summary (this session): a real billing engine gap closed, and a genuine bug caught by testing

Asked to pick the next piece myself. Checked the actual billing code first rather than guess: `generateBills` already had duplicate-generation protection (skips flats that already have a bill for the month) and the real database already had a matching `unique(society_id, flat_id, month)` constraint, both fine as they were. What wasn't fine: `Adjustment` records (credit/debit, already a real feature) never actually affected what anyone owed - they existed as records for reports, but `flatPending`/`totalPending` only ever looked at bills. That's the actual gap this session closed, along with per-flat billing overrides and a real preview step before generating bills.

**What changed:**
- **Adjustments now actually count.** A debit adjustment increases what a flat owes, a credit adjustment reduces it, and can take a flat's pending balance negative - a genuine credit, not a bug. The resident-facing dashboard and bill page now show that distinctly ("તમારી ક્રેડિટ", a green card) instead of a confusing negative rupee figure. A society-wide adjustment (not tied to any one flat) affects the total but correctly doesn't attach to any single flat's own balance.
- **Per-flat maintenance override**, a real and common need (not every flat pays the same amount). `Flat.maintenanceOverride` (and `flats.maintenance_override` in the real schema), settable from a small inline control in the Members list - honestly scoped as a focused addition, not a full flat-editing form, since that doesn't exist yet either and is bigger than this pass.
- **A real preview before generating bills.** The admin billing page now shows exactly what will be created (which flats, what each will actually be billed, any override called out) and the total, with a confirm step, instead of generating on a single click with no way to see the impact first.
- **A real bug this session's own tests caught, not something anyone reported:** `generateBills`'s returned count could be wrong (showing 0 new bills even though bills were genuinely created) under React's Strict Mode, which is actually enabled in `main.tsx`, not just a testing artifact. The cause: the old code set a variable *inside* the state-updater callback and returned it from outside, which isn't safe against React invoking that callback more than once, exactly the kind of impurity Strict Mode exists to catch. Fixed by computing the new bills from a stable snapshot before the state update, then just applying that fixed list, mirroring the pattern `addFlatsBulk` was already correctly using. Checked the rest of the store for the same pattern; nothing else had it.

**Database migration added:** `maintenance_override` (nullable numeric) on `flats`.

**Tests:** 102 passing (up from 91). 11 new, covering adjustments actually affecting pending (including the society-wide vs per-flat distinction), the override being used correctly by both the preview and the real generation, and the preview matching what generation actually does - which is exactly the test that caught the Strict Mode bug above.

**Known limitations:** the maintenance override has no dedicated edit UI beyond the one inline control described above - a fuller per-flat settings view is still ahead. One-time charges specifically (as opposed to the debit-adjustment mechanism, which already covers the same need) aren't a separate concept, and probably don't need to be. Advance balance and overpayment allocation across future bills aren't handled yet - right now a credit adjustment reflects as a balance but doesn't automatically apply itself to the next bill generated.

## Session 10 summary (this session): first slice of the big production-hardening roadmap

A comprehensive roadmap document (attached in full, covering business model, full role model, RLS rewrite, billing engine, storage, and more) came in as a single large ask. Given its own explicit 5-phase structure and the real risk of rushing security-relevant work, this session deliberately built one well-scoped, thoroughly-tested slice rather than attempting the whole thing at once. The person confirmed three things before this started: keep the owner's blanket access for now (the roadmap wanted it removed in favor of controlled support sessions, that's explicitly deferred), the ₹499 minimum and 90-day trial numbers are real and should be reflected in the product, and to proceed using good judgment on the rest.

**What changed:**
- **Real trial timers**, not just a status label. `Society.trialStartedAt` (real database column, `trial_started_at`) gets stamped once, at the moment a society is actually activated through the owner console's onboarding wizard, never earlier. `src/lib/subscription.ts` now computes trial expiry the exact same way it already computed grace expiry, no cron needed: a lapsed trial gets the same 14-day soft-landing grace window a lapsed paid subscription gets, then reads as paused. The real database's `can_write()` function mirrors this exactly, verified against a real local Postgres instance across four scenarios (fresh trial, mid-trial, just-lapsed-but-still-in-grace, fully expired) before being trusted.
- **Pricing and trial messaging actually updated** to the real numbers: 90 days free, no card required, ₹10 per flat with a ₹499 per-society minimum, with an honest example showing the minimum actually kicking in for a smaller society. Added a matching FAQ entry.
- **Two new/renamed roles**: `treasurer` (billing configuration, bill generation, payment confirmation, adjustments, receipt cancellation with a mandatory reason, the financial powers plain `accountant` doesn't have) and `auditor` (renamed from the old generic `viewer`). Both wired through the type system, the permission matrix, routing, and the real database's RLS policies (bills, payments, expenses, adjustments now all recognize treasurer). Honest limitation: treasurer currently shares accountant's existing `/accounts` pages rather than having its own - building genuinely separate pages for bill generation and receipt cancellation is part of the deferred billing-engine phase, not rushed in here.
- **Complaints split into personal and community visibility**, with real database enforcement, not just a UI filter. Verified this directly: set up two flats in a real local Postgres database, confirmed a resident can see their own flat's complaints of either visibility plus every community complaint society-wide, but never another flat's personal one. This is exactly the "residents must not receive every row and rely on frontend filtering" principle the roadmap named specifically.
- **The public lead flow no longer depends on one browser's local storage for the owner's view of it.** `Contact.tsx` now writes to the real `public_leads` table (already RLS-protected from session 7) alongside the existing Formspree email and the local demo copy, and the owner console's Leads page reads from that real table when Supabase is configured, with its own loading state. A failure on this specific write doesn't block the visitor's actual submission from succeeding, since Formspree's email delivery is what's guaranteed either way.

**Database migrations added, on top of everything already in `supabase/schema.sql`:** `trial_started_at` on `societies`; `visibility` on `complaints`; `treasurer` added to the role check constraint on `memberships`; RLS policy updates on `bills`, `payments`, `expenses`, `adjustments` (treasurer added), and a corrected `complaints_select`/`complaint_timeline_select` (real visibility enforcement, not just membership).

**No new environment variables.** Nothing here needed anything beyond what session 7-9 already required (Supabase URL/publishable key, already configured).

**Tests:** 91 passing (up from 65 at the start of this session), covering the trial timer math (26 tests across every boundary: day 89, day 91, exactly at the grace cutoff), the new role permissions (9 tests), and the leads Supabase functions (5 tests, mocked since this sandbox has no network access to supabase.co). The complaints visibility isolation and the trial write-guard were additionally verified directly against a real local Postgres instance with real test data, not just unit-tested in isolation.

**Known limitations, honestly:** the owner's blanket bypass over every society's tenant content is still there, on explicit instruction, revisit later. Treasurer doesn't have its own dedicated pages yet. The full billing engine (per-flat overrides, one-time charges, partial payments, advance balance, late fees, atomic duplicate-safe bill generation), real file storage for logos/photos/attachments, the accessibility and mobile audit, and the full required test suite (society-isolation tests, end-to-end coverage of the ten listed flows) are all still ahead, each roughly the size of this session on its own. Nothing in this session was verified against a live Supabase project directly, same standing limitation as every session before it, this sandbox has no network access to supabase.co.

## Session 9 summary: a real security fix, and closing the gap between "designed" and "actually wired to Supabase"

Another external review (ChatGPT, attached in full) checked the actual repo state rather than just the plan, and found one genuinely serious issue in session 8's self-enrollment design, plus confirmed a few things that were already fine (checked directly rather than taken on faith, one specific claim in the review, that the live site's title still showed Rajhans Tower, turned out to be wrong when fetched directly, it currently reads "Prangan One | ધ સોસાયટી OS").

- **Fixed a real vulnerability, not a hypothetical one.** Self-enrollment previously granted instant active access when the submitted phone number matched what was on file for a flat. A phone number isn't a secret, a neighbour, a former tenant, anyone who'd seen a WhatsApp group could know one. That meant someone could enter their own email alongside a real resident's known phone number and get an active membership, with the real login link going to their own inbox, not the resident's. Now matches on the pre-loaded email on file for the flat instead, since that's the actual credential being granted. No email on file means pending, always, regardless of phone. Fixed in both the local demo layer (`selfEnrollResident` in `store.tsx`) and the real database trigger (`enforce_membership_insert` in `schema.sql`), and verified against a real Postgres instance: the exact attack scenario (known phone, attacker's own email) now correctly produces `pending`, not `active`, while the legitimate case (real resident, matching email) still gets instant access.
- **Also moved role derivation server-side.** The client submitting a self-enrollment has no way to know whether a flat is owner- or tenant-occupied (`flats` isn't readable by someone who isn't a member yet), so it can't correctly pick which role to submit and shouldn't be trusted to anyway. The trigger now derives `resident_owner` vs `resident_tenant` from the flat's actual on-file occupancy, ignoring whatever the client sent. Tenant-access-disabled is checked server-side too now, not just client-side.
- **Fixed a real exposure issue in the societies table.** Making it fully publicly readable (session 8's fix for `/s/:slug` and `/join`) also meant anyone could list every society's `join_code`, `subscription_status`, `plan`, and `grace_started_at` in one query, business-sensitive fields with nothing to do with what a public share link needs. Locked `societies_select` back to member/owner only, and added two narrow functions instead (`find_society_public_profile`, `find_society_id_by_join_code`) that return only the safe fields, and only for one specific slug or code at a time, not the whole table.
- **Actually wired the real Supabase path for `/join` and `/s/:slug`**, which session 8 had explicitly left as local-demo-only. `submitJoinRequest` and `findSocietyPublicProfile` in `auth.ts` are real, callable functions now, and `Join.tsx`/`ShareLink.tsx` use them when Supabase is configured, same real-or-demo pattern as `Login.tsx`. The join submission itself calls one consolidated database function (`submit_join_request`) rather than separate lookup-then-insert-then-read-back calls from the client, since an anonymous self-enrollment can never read its own row back afterward under RLS, the database function does the whole thing with its own elevated internal privileges and just returns the resulting status.
- **Fixed an internal inconsistency the review caught**: the previous session's owner design fix (global owner, `society_id` null) only touched `schema.sql`, not the code that reads a claimed membership back. `AuthCallback.tsx` and the `ClaimedMembership` type now correctly handle a null `societyId` (falls back to a placeholder for the local session shape, which always expects a real string; `session.role === 'owner'` bypasses per-society scoping everywhere anyway, so the placeholder value itself doesn't matter functionally), and the multi-society picker shows a sensible label instead of a failed join lookup.
- **The person is setting up Resend** for real email delivery, replacing Supabase's default rate-limited sender. That's a dashboard configuration step on their end, not something to build here.

**What's still explicitly not done**: the full owner-dashboard six-section reorganization (still cosmetic, not functional, still deliberately deferred). Google login (still deliberately behind proving email end to end first). The broader table-by-table data migration (bills, payments, complaints, etc. still fully local-only) - the review's suggested build order for that (owner login → society creation → admin invitation → resident join → committee approval → second-society isolation test) is sound and is the right next phase once the identity pieces above are confirmed working against the live project.

## Session 8 summary: the actual identity/onboarding architecture, not just login

The person and an external review (ChatGPT, attached in full) both converged on the same real problem: this project had a login mechanism but no actual identity model behind it. This session built that model, end to end, following the "who are you, which society, what role, straight to the right place" architecture both reviews independently landed on, with one deliberate deviation stated clearly below.

**The core resolution, finally wired:**
- `Session` gained `explicitSociety` (already existed) working alongside a real resolution path now: `AuthCallback.tsx` at `/auth/callback` (the route the magic link already pointed at, which genuinely didn't exist as a page until now) waits for the real authenticated user, calls `claimMemberships`, and either lands them straight in the right dashboard (one membership), shows a society picker (more than one), or sends them to the new `/no-access` page (none).
- `claimMemberships` in `auth.ts` now also returns the society's real name (needed for the picker, a raw id isn't something to show anyone), and only claims `status = 'active'` rows, a pending self-enrollment isn't claimable until approved.
- **Deliberately did not put Google login first**, despite the attached review recommending it. Reasoning: this is the one part of the whole project that can't be verified from this sandbox (no network access to supabase.co), adding a second unverified external integration (a separate Google Cloud OAuth setup) on top of the first felt like compounding risk rather than reducing friction. Get email + resolution proven first, add Google next to it once that's confirmed working.

**Resident self-enrollment (`/join`), Google Classroom's class-code pattern, adapted:**
- `Society.joinCode`, a short generated code, shown and copyable from the owner console's society detail page.
- A resident enters the code, their flat number, name, phone, and email. Matched against what's on file for that flat - if the phone matches, they're in immediately; if not, it's a pending request a committee member approves with one tap (new card in `admin/Members.tsx`).
- **This matching decision is enforced server-side**, not just in the local demo's JS. `supabase/schema.sql` got a real `before insert` trigger (`enforce_membership_insert`) that ignores whatever status a self-enrollment request claims and decides active-vs-pending itself by checking the flat's phone on file. Verified this by actually installing Postgres 16 in this sandbox and running the real trigger against real test data, not just reasoning about it, see the specific scenarios below.

**Three real bugs the Postgres testing caught, that would not have been caught any other way:**
1. `Society.slug` (the `/s/:slug` feature) had never actually been added to `schema.sql` at all - the real database was missing a column the app has depended on since that feature was built. `join_code` needed the same column, added both.
2. `memberships` had no `update` policy whatsoever. RLS defaults to deny with no matching policy, meaning `claimMemberships`' own claim-by-email update, and any future approval action, would have silently failed against a real project. Added `memberships_claim` (claim your own pending invite, only ever to your own uid) and `memberships_manage` (committee/owner managing memberships in their society).
3. The self-enrollment trigger's own bootstrap path was initially broken: it checked `has_role()`, which requires an *existing* privileged membership to already exist, exactly the thing that doesn't exist yet when manually bootstrapping the very first owner/admin row through the Supabase SQL editor. Fixed by checking `session_user` (not `current_user`, which reports the function's owner inside a `security definer` function, not the caller) against `postgres`/`service_role`/`supabase_admin`, so a direct database connection bypasses the self-enrollment restrictions while the public API (`anon`/`authenticated` roles) still gets them enforced. Verified this exact distinction with real `SET SESSION AUTHORIZATION` role switches, not just assumed.
4. `societies_select` was member-only, which quietly broke both `/s/:slug` and `/join` against real RLS - a visitor isn't a member of anything yet, so they could never look up a society by slug or code in the first place. Made societies fully publicly readable (nothing in the table is sensitive; all of it already shows on the public app or public share pages anyway).
5. `flats`, deliberately, did NOT get the same treatment - a flat row has owner/tenant names, phone, email, broadening that would leak every resident's contact info to anyone who knows a join code and guesses a flat number. Added a narrow `find_flat_for_join()` function instead: security definer, returns only an opaque id, never the row.

**The "no membership found" page, done as the marketing moment it should be, not a dead end:** `/no-access`, using the more precise wording from the attached review ("we couldn't find a society linked to this email yet," which doesn't imply their society isn't on the platform, just that this particular email hasn't been added) - offers the join-code path and the "bring my society here" path (straight into the existing Contact/Formspree pipe), and logs the attempt (email only, into a new owner-only `unmatched_login_attempts` table) so the owner console shows it as a real interest signal, grouped by email with a count, kept separate from the sales lead inbox since nobody agreed to being contacted about it.

**Owner support-mode refinements**, per the attached review's "require a reason, show a persistent banner" recommendation: `enterSociety` now takes an optional `reason`, required via a modal before entering write-capable support (read-only stays a single click, no reason needed), logged on the `ImpersonationLog` and shown in the owner's Activity page. A persistent, hard-to-miss saffron banner now shows across every admin/accountant screen while the owner is actively in support mode, with a one-click exit.

**What's still explicitly not done, on purpose:** Google login (see above). The full owner-dashboard six-section reorganization the review suggested - scoped out of this pass deliberately, it's an organizational/cosmetic change, not a functional gap, and this session was already large. The actual Supabase-backed version of `selfEnrollResident` (the local demo layer has the real, tested matching logic; the database has the real, tested trigger enforcing it; the JS function that calls the real Supabase insert for this specific flow doesn't exist yet, same bucket as the rest of the "swap the write actions" phase).

## Session 7 summary: real Supabase credentials arrived, contact form actually delivers now

The person provided real values for the first time: a Supabase project URL and a publishable key, plus a Formspree endpoint (https://formspree.io/f/mnjklkwp) for the contact form. Important limitation to record here: this session's environment has no network access to supabase.co or formspree.io, so nothing below could be tested against the real services directly, only written carefully and verified everywhere it's possible to verify (compile, build, and mocked-fetch unit tests). The actual live connection needs to be confirmed by the person, or by a session that does have that network access (Claude Code, or just `npm run dev` locally with the real `.env` values).

- **Caught a real naming mismatch before it caused a silent failure.** The values were given as `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, Next.js convention. This app is Vite, which only exposes env vars prefixed `VITE_`. Had these been added to Cloudflare Pages as-is, the app would have silently kept running on the local data layer with no error, since `supabaseConfigured` would just read as false forever.
- **Also confirmed via a live search that "publishable key" is Supabase's current, correct terminology** (replacing the legacy "anon key," being phased out through 2026), not a mistake or a different product. Same purpose, safe to ship in client code, protected by RLS not secrecy. `src/lib/supabase.ts` now reads `VITE_SUPABASE_PUBLISHABLE_KEY` primarily, with `VITE_SUPABASE_ANON_KEY` accepted as a fallback for older references. Updated `.env.example` and `supabase/README.md` to match throughout.
- **The "Exact prompts for the next Claude Code session" section was inaccurate and has been corrected.** Prompt 1 (Supabase client + magic link) was marked as still-to-do; it's actually done, real code, since session 4/5. Prompt 2 (membership claim) was described as if nothing existed; `claimMemberships` in `src/lib/auth.ts` already exists, what's actually missing is narrower: nothing calls it yet after a successful login. Rewrote both to reflect reality, and added an explicit "stop and verify isolation with a second real society" instruction inside the per-table read-swap prompt (step 3), not just at the very end, so a future session can't accidentally swap several tables before the first real isolation check.
- **The contact form now actually delivers, not just avoids a fake success.** `src/lib/formspree.ts` posts to the real Formspree endpoint; `Contact.tsx` shows a real sending state and a real error state (never a fake "thanks" if the network call fails), and still calls `addLead()` afterward so the owner console's leads inbox keeps showing every submission too, Formspree is the delivery, `addLead` is a local mirror, not a replacement. Added a correction note on the session 5 log entry that had overstated this as already solved, it wasn't, `addLead` alone never reached anyone.
- **WhatsApp stays deferred**, on explicit confirmation, no change needed there.

**What's next, and it's the real remaining task**: apply `supabase/schema.sql` in the Supabase SQL editor (step 0 in the prompts section below), confirm RLS shows enabled on every table, then work through the membership-claim step and the table-by-table read/write swap, verifying isolation with a second real society after the very first table, not at the end.

## Session 6 summary: login architecture fix, demo/login split, mobile fixes, and the rest of the documentation cleanup

Another external review (this time checking the actual live site plus real device screenshots, not just the code) caught two genuinely important things the previous sessions missed, both confirmed by reading the real code before touching anything, not just trusted on faith.

- **Fixed the real bug: `/login` was showing Rajhans Tower's branding to every visitor.** Root cause: every fresh session defaults to `societyId: soc_rajhans` (reasonable, other pages need *some* active society to render against), and Login.tsx blindly rendered whatever society was active. Fix: `Session` gained an `explicitSociety: boolean` flag, true only when a society was chosen on purpose (a `/s/:slug` share link, a resolved membership, a demo flat pick), false for a session just sitting on the raw default. Login.tsx now shows Prangan One's own generic branding unless that flag is true. Didn't touch the default society resolution itself, that stays as-is since too much else depends on it.
- **Split demo mode onto its own `/demo` page, out of `/login` entirely.** The real login screen now only ever has the honest email flow, never a "become the owner" button, regardless of `VITE_DEMO_MODE`. `/demo` has its own gate (the same flag) and its own honest "not available" state when it's off.
- **Fixed a real mobile bug: there was no way to reach `/login` from a phone.** The desktop "Log in" button was `hidden` below the `sm` breakpoint, and the mobile hamburger menu never got a login link added when it was built. Confirmed by reading the actual JSX, not just the screenshot. Fixed: the mobile menu now leads with a prominent login button.
- **Public header/footer show the full wordmark at every size now, including mobile.** Previously mobile showed the bare symbol with no name at all, which defeats the point of brand recognition on the exact surface most people will meet it on first.
- **Public site now defaults to Gujarati, English is the toggle-in option.** This reverses the original roadmap's English-default assumption, on the person's explicit instruction, since real visitors are finding this through WhatsApp shares and word of mouth in Gujarati. `index.html`'s static fallback (what shows before any JS runs) was updated to match.
- **Prangan One branding is now genuinely visible on resident, admin, and accountant screens, with the name, not just the mark**, on the explicit instruction that this is a deliberate brand-recognition decision, not an oversight: a thin "Powered by [wordmark]" strip above the resident header, a properly-sized (and finally *visible*, see below) lockup at the bottom of the admin/accountant sidebar, and a small mark in the admin/accountant mobile header. Society identity stays primary in all three, this is a visible secondary signature, not equal billing.
- **Fixed a real contrast bug found while doing the above**: the admin/accountant sidebar's old "Powered by Prangan One" line used the navy-colored mark on a navy-900 background, nearly invisible. `PoweredByPrangan` now takes a `dark` prop and shows the real wordmark image (not just the symbol plus repeated text), so the name stays legible on both light and dark surfaces.
- **Fixed the FAQ's premature claim** ("isolated at the database level") to honestly describe the current state (browser-local, no shared backend yet) and what's coming, instead of a claim that wasn't true yet.
- **Removed the Contact page's broken WhatsApp button** (linked to bare `wa.me` with no number), softened the copy to only mention email until a real number exists. One-line comment marks exactly where to add it back.
- **Pricing page**: softened "WhatsApp reminders" to "one-tap WhatsApp reminder sharing" (matches what actually exists, share links, not automation), and added two concrete examples (24 flats = ₹240/month, 100 flats = ₹1,000/month) addressing the "looks unfinished, too much empty space" feedback with actual content instead of decoration.
- **Finished the documentation cleanup flagged as unsorted last session**: README's owner-console description, payment-flow description (was calling the real manual+pending-confirmation flow a "placeholder", it isn't one), and Next-steps ordering (Supabase work is now clearly "active", Razorpay/WhatsApp API clearly "deferred") are all fixed. Removed the closing Essancia attribution line, no legal requirement for it and it doesn't fit a standalone product identity. `docs/DESIGN_SYSTEM.md`'s stale `/saas/*` references fixed. `robots.txt`'s unnecessary `/home` allow removed (it only redirects) and `/demo` added to the disallow list. `.env.example`'s Razorpay/WhatsApp placeholders removed entirely, replaced with a plain explanatory comment, no vendor names. Found and fixed two *currently-actionable* stale file paths inside `CLAUDE_CODE_NEXT_STEPS.md`'s own ready-to-paste prompts (they still said `src/pages/saas/...`, which would have sent a future session to a path that doesn't exist), left the historical session-log entries alone, added one line at the top noting explicitly why those are historical, not current, so nobody mistakes one for the other later.

**What this pass deliberately did not touch**: no redesign, no new dependencies, no Supabase work, no change to the working resident/admin/accountant product screens beyond the branding additions explicitly requested, no change to the official brand asset files themselves.

## Session 5 summary: official brand assets, route correction, and pre-Supabase production cleanup

The repo went live on GitHub (https://github.com/optimisticjp/Prangan-One) between sessions, complete with a third-party Claude Code skills template layered on top (`.claude/skills/`, `CLAUDE.md`, `INSTALL_REPORT.md`, none of it from this project). This session worked directly against a fresh clone of that real repo, not a stale local copy, verified the baseline first (`npm ci`, `npm run build`, `npm test`, all passing before touching anything), then did a focused cleanup pass. Another AI's review (ChatGPT) proposed the task list; each item was independently checked against the actual code rather than taken on faith, one item (the `/`-is-login vs `/`-is-home question) turned out to be a genuine correction back to something already locked in `docs/PRANGAN_ONE_ROADMAP.md` section 10, not a new opinion.

- **Real brand assets, not a placeholder.** The official logo/symbol/favicon/PWA files now live in `public/brand/` and `public/`. `src/components/PranganBrand.tsx` replaces the old hand-drawn `PranganMark.tsx` (deleted, nothing references it anymore), with `wordmark-navy`/`wordmark-white`/`symbol-navy`/`symbol-white` variants at the real asset aspect ratio, plus a `PoweredByPrangan` helper for receipts and the admin sidebar footer. Verified the real brand navy directly from the SVG source (`#10223F`) rather than trust the claim, it checked out, added as its own fixed `brand-navy` Tailwind token, separate from the tenant-themeable `navy` scale, used only on platform-owned surfaces (owner sidebar, public site, error pages), never forced onto a tenant's own theme.
- **Route structure corrected**: `/` is the public homepage, `/login` is login, matching the roadmap's original locked spec. `/home` redirects to `/` so no old link breaks. Every `Navigate`/`Link`/logout call site that assumed the old structure got updated, `RoleGate`, the admin/accountant Shell, resident Profile, the owner sidebar, 404/403/error boundary.
- **Society share links**: `pranganone.com/s/{slug}` (Rajhans Tower is `/s/rajhans-tower`) shows that society's branding, then hands off to `/login` with it set as context. Unknown slug gets a clean not-found state, nothing about which slugs exist leaks either way. `Society.slug` is auto-generated and kept unique when a new society is created via the onboarding wizard.
- **Demo mode is now a real, separate concern from production.** `VITE_DEMO_MODE` (default: on in `import.meta.env.DEV`, off otherwise) gates the resident/admin/accountant/owner shortcut buttons on `/login`. When Supabase isn't configured and demo mode is off, the email box shows an honest "login service is being configured, contact your committee or care@pranganone.com" message instead of ever simulating a sent email.
- **The Razorpay "coming soon" promise is gone.** Resident bill payment now says only that payment is confirmed by committee, no gateway timeline implied, matching the honest-wording principle everywhere else.
- **Language metadata**: `index.html` defaults to `lang="en"` (matching `/` now being the English-default public homepage); `usePublicLang` syncs `document.documentElement.lang` to the toggle and restores `gu` on unmount; every app shell (`ResidentLayout`, `Shell`, `OwnerLayout`, `Login`, `ShareLink`) calls a new `useAppLang()` hook that forces `gu` on mount, so a direct/bookmarked link into the app is never stuck showing the public site's English default. Added `og:site_name` alongside the existing OpenGraph tags.
- **The vite/vitest warning was investigated, not silently left.** Root cause: `vitest@4.1.9` carries its own internal `vite@8.1.3` (which prefers the newer `oxc` transform) as a transitive dependency, separate from this project's own pinned `vite@5.4.21` (which the actual `npm run build` uses, and which never shows the warning). Fixing it for real would mean either upgrading the project's own Vite to 8.x or downgrading Vitest's major version, both real, unnecessary risk for a cosmetic test-runner-only warning that doesn't affect the production build. Left as documented, harmless technical debt, per instruction.
- **Docs updated** (`README.md`, `QUICK_START.md`, `.env.example`, this file) for the corrected route structure and the new demo-mode flag; Razorpay/WhatsApp env vars in `.env.example` are now explicitly labeled deferred, not active setup steps.
- **New tests**: home page renders at `/`, login renders at `/login`, `/home` redirects to `/`, demo shortcuts are absent when `VITE_DEMO_MODE` is off, society slug resolution finds the right society, an unknown slug shows the not-found state. Alongside the existing subscription and CSV suites.

**What this pass deliberately did not touch**, matching the instruction it came with: no Supabase integration, no redesign, no new major modules, no visitor/gatekeeper, no payment gateway code, no WhatsApp Business API, no AI features, no major dependency upgrades.

## Session 4 summary: the actual implementation, not just the plan

This is the session that stopped scoping down to "step 1 only" and implemented as much of `docs/PRANGAN_ONE_ROADMAP.md` as is honestly possible without a live Supabase project. Everything below is real, compiling, tested code, not a plan for code. Where something genuinely can't be verified without a live database (RLS policies specifically), that's called out explicitly rather than blurred.

- **Full role model.** `Role` is now `owner | society_admin | committee_member | accountant | resident_owner | resident_tenant | viewer`, matching the roadmap exactly. Every route gate, nav array, and permission check in the app was updated, not just the type. `src/lib/permissions.ts` has `tenantCapabilities(mode)` mapping the three tenant-access levels (disabled/limited/full) to what a tenant can actually do, and `canManageBilling()` for the committee_member billing restriction.
- **Real subscription lifecycle with a real write-guard.** `src/lib/subscription.ts` is pure, dependency-free logic (`canWrite`, `effectiveStatus`, `graceDaysRemaining`, `statusBanner`), covered by 14 real unit tests that actually run (`npm test`). The 14-day grace window is computed from a timestamp at check-time, not a scheduled job, so there's nothing that can silently fail to fire. `store.tsx`'s `guardedSetDb` routes every mutating action through this check; blocked writes are a defensive no-op locally, and the same rule is written into every insert/update RLS policy in `supabase/schema.sql` via a `can_write()` Postgres function, so enforcement doesn't depend on the frontend once Supabase is connected.
- **Module system, both layers.** `Society.modules` is now `{ ownerEnabled, adminVisible }`. The owner console (`/owner/societies/:id`) controls the first layer; the society's own Settings page now controls the second, closing the gap flagged twice in earlier sessions. `ModuleGate` shows an explicit "this feature is not active for your society" message instead of a silent redirect, per the roadmap's non-negotiable on this.
- **The owner backend is real**, not a preview. Moved from `/saas` to `/owner/*` (old links redirect, don't 404): a dashboard with the exact stats the roadmap asks for (societies by status, total flats, total memberships, expected monthly revenue, pending platform payments, recent activity), a societies list with inline subscription controls, the onboarding wizard, a per-society detail/edit page with subscription lifecycle buttons and membership management, a manual platform billing tracker (₹10/flat/month, editable per society), a leads inbox, and an activity log showing both the audit trail and every owner impersonation session (read-only vs write-capable, both logged, per the roadmap's non-negotiable).
- **Real email magic-link auth code.** `src/lib/supabase.ts` creates a real Supabase client, `null` until `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY` exist (so this sandbox, with no live project, still boots and stays verifiable). `src/lib/auth.ts` has the actual `signInWithOtp` call and the membership-claim query against the schema. `Login.tsx` calls the real function when Supabase is configured, and only falls back to the old "check your email" simulation when it isn't, which is genuinely the case right now.
- **CSV member import, for real.** `src/lib/csv.ts` has a hand-written CSV parser (quoted fields, embedded commas/newlines, escaped quotes) and a row-level validator, both covered by 12 real unit tests. The Members page has a full import flow: pick a file, see valid rows and specific per-row errors, confirm, get told exactly how many were added and which flat numbers were skipped as duplicates.
- **Receipt cancellation and pending-payment confirmation.** Payments can now be cancelled with a required reason (never deleted, `cancelReceipt` reverses the bill credit and logs to `audit_logs`). Residents have an honest "I have paid" flow (`recordPayment({ pending: true })`) that explicitly says it isn't an official payment; the committee sees a confirmation queue and approves it, which is when the real receipt gets generated.
- **Route-based code splitting, measured, not just claimed.** Every page in `App.tsx` is `React.lazy`. Main bundle dropped from 482KB to 320KB in the actual build output; residents no longer download admin/accountant/owner code.
- **Error boundary, 404, 403.** `src/components/ErrorBoundary.tsx` catches render errors with a real recovery action instead of a blank screen. `RoleGate` now distinguishes "not logged in" (goes to login) from "logged in with the wrong role" (goes to a real `/403` page) instead of bouncing both to the same place.
- **Accessibility on the shared Modal.** Real focus trap (Tab cycles inside the dialog), Escape closes it, focus moves in on open and restores to the trigger element on close, proper `role="dialog"`/`aria-labelledby`. This is the one shared component every form in the app uses, so it was worth doing properly rather than per-page.
- **The public marketing site exists**, bilingual (English default, Gujarati toggle, backed by `localStorage` so the choice persists across pages), at `/`, `/features`, `/pricing`, `/faq`, `/contact`. The contact page's lead form is wired to a real `addLead` action, not a fake form. *(Correction added in session 6: `addLead` alone only ever wrote to the local browser store, nobody actually received a submission until Formspree was wired in, see session 6's summary.)* FAQ and homepage both carry real JSON-LD structured data (FAQPage, SoftwareApplication). `robots.txt` plus a default `noindex` meta tag (overridden only by the public pages) keeps the actual app out of search results.
- **`supabase/schema.sql` rewritten comprehensively**: the full 7-role constraint, the `can_write()` function used inside every write policy, tenant-access-aware SELECT policies (a `limited`-mode tenant can't read documents, vehicles, or restricted events), and four new tables (`platform_billing`, `public_leads`, `audit_logs`, `impersonation_logs`) with their own policies. This is 816 lines of genuinely careful SQL. It is also, honestly, completely unverified against a real Postgres instance, because none exists yet in this environment. Structurally sound by static inspection (balanced parens, matched quotes, no obvious typos), but "written carefully" and "tested" are different claims, and only the second one is really true once a live project exists and the roadmap's own hard checkpoint (create a second test society, try to break isolation) actually gets run.
- **Essancia references removed from user-facing docs and copy** where they described this as an Essancia-branded thing rather than genuine authorship attribution (which stayed, since removing "built by X" from your own project documentation isn't what "remove ownership branding" was asking for).

**Still not done, named specifically so nothing is quietly dropped:** the actual Supabase project doesn't exist (can't be created from this chat, needs your account), so nothing above has been run against a live database, only against the local write-guard and the local data layer. File storage integration (Storage buckets, real upload) is still metadata/data-URL only. The `resident_owner`/`resident_tenant` split exists in the type system and route gates but Login's demo shortcut still derives it from the flat's `occupancy` field rather than a real per-user membership lookup (correct behavior for the demo, becomes real once auth is connected). CSV import for platform_billing/leads/memberships bulk-loading doesn't exist yet (only flats). Storage-backed logo upload, WhatsApp Cloud API, and Razorpay remain deferred exactly as the roadmap says.

## Session 3 summary: rebrand to Prangan One

A locked, more detailed roadmap document arrived (worth reading in full if it's still in the repo/uploads: it renames the product, sharpens the "committee is the hero" positioning, adds a formal subscription lifecycle, a richer role model, and a full build order). This session executed step 1 of that roadmap, the rebrand pass, everything doable without a live Supabase project or the pranganone.com domain, both of which need the person's action outside this chat.

- **Killed every hardcoded-Rajhans leak in reusable logic**, confirmed by actually grepping the code, not just trusting the roadmap doc's claims (they were all accurate): `whatsapp.ts`'s five message templates now take `societyName` as a real parameter instead of hardcoding "રાજહંસ ટાવર", the resident Profile footer and `copy.ts`'s `appName` now reflect the actual active society / the platform name instead of a hardcoded string. Every one of the 8 call sites across 6 pages plus `ReceiptView.tsx` was updated to pass `society.name`.
- **Self-hosted the fonts.** Removed the Google Fonts CDN `<link>` tags from `index.html`, added `@fontsource/noto-sans-gujarati` and `@fontsource/inter` (weights 400/500/600/700), imported at the top of `index.css`. No more third-party request on every page load, and no resident IPs leaked to Google, which matters for ever claiming "data in India."
- **Seed data now sits behind an env flag.** `VITE_DEMO_SEED` (default true, so `npm run dev` with no `.env` still shows a working demo) gates whether `store.tsx` loads the Rajhans/lead sample data or a minimal single-placeholder-society empty state. See `emptySeed()` in `store.tsx`. This is a stopgap for the localStorage era specifically, it stops being relevant once the Supabase swap lands, since a fresh Supabase project is naturally empty.
- **New platform identity, separate from any tenant's.** `PranganMark` (`src/components/PranganMark.tsx`) is a new courtyard-motif mark (prangan = courtyard: a compound wall with an open courtyard at its center), used for the owner console's own top bar and the static PWA icons (`public/icon.svg`, `public/icon-maskable.svg`). This is deliberately separate from `SocietyLogo`/`SocietyBadge`, which still correctly gives Rajhans Tower its own swan mark as that society's specific brand. Don't merge these two, one represents the platform, the other represents whichever tenant is active.
- **"Powered by Prangan One"** added to receipts (inside the print area, so it shows on printed/PDF receipts too) and the admin/accountant sidebar footer. Login's hero now reads "Prangan One પર ડિજિટલ સોસાયટી" alongside the active society's own name.
- **Fixed a not-yet-true claim.** The owner console's trust-signal row said "data in India" before the Supabase region is even set. Changed to "built for India" until that's actually true, matching the roadmap's own non-negotiable on this point.
- **package.json, index.html, and manifest.webmanifest** renamed from Rajhans-specific to Prangan One as the platform-level identity (the static PWA manifest can't be tenant-specific in one shared deployment, so it represents the platform; the in-app experience after login stays tenant-branded via the existing theme/logo system).

**Not done in this pass, and deliberately so:** the deeper role model (`society_admin` vs `committee_member` split, `resident_owner` vs `resident_tenant` as distinct roles), the subscription lifecycle (trial/active/grace/paused/archived) and its 14-day grace-to-paused window, the admin-layer module toggle (letting committees, not just the owner, show/hide resident-facing modules), CSV member import, receipt cancellation, owner impersonation audit logging, and the bilingual public marketing site. All of these are real, called out explicitly in the roadmap doc, and are bigger than a rebrand pass, they come after the Supabase foundation work below, per that document's build order.

## Session 2 summary: the SaaS configuration layer

Built entirely on top of the session-1 localStorage demo, still no live backend connected. What's new:

- **Real multi-tenancy in the data layer.** `src/lib/store.tsx` now scopes every table to the active society (`session.societyId`) before handing `db` to pages, except `db.societies` which stays the full platform-wide list (the owner console needs to see every tenant). Receipt numbering moved from one global counter to a `receiptSeq` + `receiptPrefix` per society. This required no changes to any of the 32 existing pages: they still just call `useData()` and read `db`/`society` the same way.
- **A real theme engine, not a color picker.** Six preset themes (see `src/lib/theme/presets.ts`), each a full navy + saffron color scale with the same lightness steps as the original palette, so contrast quality stays consistent across every preset. Applied at runtime via CSS custom properties (`src/lib/theme/apply.ts`) that every existing `bg-navy-800`/`text-saffron-600` class already reads through Tailwind's `rgb(var(...) / <alpha-value>)` pattern, so no page needed to change. Deliberately not a free hex-code picker: a treasurer can't judge contrast on sight, so the choice is curated instead of arbitrary. `cream` (background) and the semantic status colors (paid/pending/overdue) never change per theme, on purpose.
- **Module toggles, enforced in the router, not just the UI.** `src/lib/types.ts` has `SocietyModules` (billing, complaints, notices, documents, vendors, polls, events, parking, reports). `src/components/ModuleGate.tsx` redirects away from a disabled module's routes even on direct URL access. Nav arrays (admin sidebar, resident bottom tabs, the More page) filter out disabled modules too. No visitor/gatekeeper key exists anywhere in this system, on purpose, still out of scope.
- **A 5-step onboarding wizard** (`src/pages/saas/Onboarding.tsx`): details, branding, billing setup, modules, review. Everything defaults on, theme presets are tap-to-select cards, logo upload is optional with a generated-initials fallback. Creates the society and drops the owner straight into its admin panel.
- **A real owner console**, not just a preview (`src/pages/saas/Preview.tsx` + `src/pages/saas/SocietyDetail.tsx`): societies table links to a real edit page per society (branding, modules, plan/status), plus an "enter this society as its admin" support action for Essancia to help a client directly.
- **Login reworked for email magic link**, per the decision below: `src/pages/Login.tsx` now leads with a real email input (currently ends at a "check your email" demo state, since there's no live backend to actually send from), with the old role/flat picker kept as a clearly-labeled demo shortcut underneath, not removed, since Essancia still needs it for pitches until the real backend is live.
- **`supabase/schema.sql` updated**: `societies` gained the tenant fields (city, area, theme_key, logo_url, receipt_prefix, modules jsonb). `memberships` reworked for the email-claim flow (see "Auth decision" below) instead of assuming a user_id exists from day one.

## Decisions log

**Session 2 additions:**

- **Cloud hosting: Cloudflare Pages, not Vercel.** Verified this directly rather than assume: Vercel's free Hobby plan is restricted by its own terms to non-commercial, personal use, and this becomes a paid client engagement the moment it's set up for Rajhans Tower. Cloudflare Pages' free tier explicitly allows commercial use, with unlimited bandwidth and no credit card required. Same git-deploy workflow either way.
- **Auth: email magic link, not phone OTP.** The person chose this explicitly after being shown the cost tradeoff. Real phone OTP in India needs TRAI DLT sender registration (roughly ₹5,900 + GST one-time, 3 to 7 working days to clear) plus a per-message cost, none of which is actually free. Supabase's built-in email magic link needs no separate provider and costs nothing on the free tier. The tradeoff: this needs residents to have and check an email address, which won't be true for every member, especially older ones. The Members page now collects an optional email per flat (owner and tenant separately) for exactly this reason, and CLAUDE_CODE_NEXT_STEPS keeps a fallback path where the committee can act on a resident's behalf if that resident never gets a working login.
- **Supabase free tier's 7-day auto-pause is a real operational risk**, not a footnote. A free project with no traffic for seven straight days pauses itself until someone manually resumes it from the dashboard. For a live committee's app, a quiet week (a holiday, low adoption at first) would mean the app just looks broken. Fix: a scheduled free keep-alive ping (GitHub Actions cron or an UptimeRobot check) every few days. This needs to be set up as part of the go-live checklist, see supabase/README.md.
- **Not everything is configurable.** Explicitly rejected a fully generic "any field, any color, any layout" system. Societies control branding (curated theme + logo), setup (maintenance amount, due day, receipt numbering), and which modules are on, nothing else. Product structure, page layout, and the core Gujarati copy stay fixed platform-wide. This was the person's own instinct going in, and it's held throughout the build.
- **Designed for the committee's actual skill level, not a typical SaaS admin's.** A Rajhans-style treasurer or secretary is comfortable with WhatsApp and a UPI app, not with SaaS jargon. Concretely: the onboarding wizard defaults everything to sensible values rather than starting blank; internal vocabulary (module, tenant, theme key) never appears in any user-facing copy, only in code comments and this file; the branding step is tap-to-choose preset cards, never a hex input; every module toggle has a one-line plain-language reason, not just a name. The SaaS owner console is the one deliberate exception, it can stay more technical since it's Essancia's own tool.
- **Self-serve-ready architecture, Essancia-operated for now.** The person wants the option of societies signing themselves up eventually, so the onboarding wizard and data model don't assume Essancia is always the one filling in the form. Nothing today exposes that signup flow publicly though: `/saas/*` still sits behind the `owner` role gate. Making it genuinely public is future work (see Roadmap), and would need real validation, spam/abuse protection, and probably a lightweight approval step before a self-signed-up society goes fully active, none of which exists yet.
- **Committee contact management stayed out of scope this session.** There's no admin UI yet for adding/editing the committee contact list (`db.contacts` is still seed-data-only, no `addContact`/`updateContact` action exists in the store). The onboarding wizard only collects a single support phone number on the Society record itself. Flagged here rather than silently skipped, this is a small, contained follow-up whenever it's needed.

**Session 1 decisions** (unchanged, still accurate): localStorage over a mock backend for the first pass so the committee could react to the UI before any backend investment; no gate/visitor security module, explicitly out of scope; spoken Surat Gujarati throughout, not textbook Gujarati; green/amber/red as the only status language app-wide; WhatsApp via `wa.me` share links rather than the Cloud API, since the Cloud API needs a verified Meta Business account, a real setup project on its own, while share links work today for free; document and complaint-photo uploads are metadata-only until real file storage is connected.

## Current state

**Fully working today, no caveats:** everything from session 1 (all 4 role flows, CRUD everywhere, CSV exports, printable receipts, one-vote-per-flat, duplicate-parking-slot warning, AMC expiry warnings), plus from session 2: theme switching, module toggles enforced at the router level, the full onboarding wizard, and the owner console's create/edit/enter-as-admin flows. All of it runs against local demo data and was verified with a fresh `npm install` + `npm run build` + route-by-route smoke test.

**Simulated / local-only:** all data still lives in browser localStorage; the email magic-link login gets to a "check your email" state and stops there, since there's no live backend to send from yet (a small demo-mode note says as much, and the old role-picker still works underneath for demos); logo uploads become a data-URL that lives in localStorage, not a real file, so a logo set today won't survive the eventual Supabase migration as-is (see the swap plan below); WhatsApp buttons still open a share picker; the payment gateway button is still a visual placeholder.

**Needs Supabase (and Cloudflare Pages) to become real:** literally everything now, since this is the actual next step, not a someday item. See below.

## Assumptions made, worth confirming with the committee before a real pilot

Same as session 1, still open: ₹1200/month maintenance amount and the 10th due-date are placeholders for Rajhans Tower's actual figures; one flat = one vote on polls regardless of size; whether tenants should ever be billed directly instead of through the owner.

**New this session:** every resident's login now depends on having a working email address on file. Before a real pilot, the committee needs a plan for members who don't have one or don't check it, at minimum, the committee should be able to record a payment or file a complaint on a resident's behalf (already possible today, since admin/accountant already have full access to every flat's data) so a missing email doesn't lock someone out of the system entirely, just out of self-service.

## Roadmap

**`docs/PRANGAN_ONE_ROADMAP.md` is now the authoritative build order**, locked and more detailed than the list below (it has the full foundation-then-continuous build order, the subscription lifecycle, the richer role model, and the non-negotiables). The list below is the older, superseded version, kept for the historical reasoning behind decisions like Cloudflare Pages and email magic link, which the new doc kept unchanged.

1. **Connect Supabase for real.** The single biggest remaining step, see the swap plan below.
2. **Deploy to Cloudflare Pages.** Static build, git-connected, free, commercial use allowed.
3. **Real email magic-link auth**, replacing the current "check your email" dead end.
4. **Migrate uploaded logos from data-URLs to Supabase Storage**, so they survive across devices and don't bloat localStorage.
5. **Committee contact management UI** (the small gap flagged above).
6. **Razorpay, UPI-first**, once there's real usage to justify the KYC and per-transaction cost. Not a launch blocker: manual payment recording already mirrors how the treasurer works today.
7. **WhatsApp Cloud API**, once there's a verified Meta Business account. Not a launch blocker either.
8. **Public self-serve society signup**, if and when that's actually wanted, with real validation and abuse protection. The Prangan One roadmap currently says owner-created only, no public self-signup, worth confirming that's still the intent before building this.

## The Supabase swap: how it fits together

Unchanged in spirit from session 1: `src/lib/store.tsx`'s `useData()` hook is still the only thing pages touch, and its public shape (the field and action names) hasn't changed, only its internals need to change. What's different now that the SaaS layer exists:

1. **Auth first, using Supabase's built-in email magic link** (Authentication → Providers → Email, "Enable Email OTP" style passwordless sign-in is on by default, no extra provider setup needed, unlike phone). Replace `Login.tsx`'s email step so it actually calls `supabase.auth.signInWithOtp({ email })` instead of just flipping local `sent` state.
2. **The membership-claim step.** Because a resident's `memberships` row is created by the committee (via the Members page, using the email captured there) before that resident ever logs in, the first successful magic-link login needs to match `auth.users.email` to a pending `memberships` row and attach the real `user_id`. Cleanest place for this: a Supabase Edge Function triggered on new `auth.users` inserts, or a client-side "claim" query run once right after first login. Either way, this is new work, session 1's plan didn't need it because it assumed phone OTP with a different membership model.
3. **Read path, table by table**, same order as before: `societies` → `flats` → `notices` → `documents` → `vendors` → `bills`/`payments` → `complaints` → `polls`/`poll_votes` → `events` and its 3 sub-tables → `vehicles`/`contacts`/`adjustments`. The scoping-by-active-society logic already lives in one place in `store.tsx`, so each table's swap is: replace that table's slice of local state with a Supabase query filtered by RLS (no explicit `.eq('society_id', ...)` even needed once RLS is on), keep the shape matching `types.ts` exactly.
4. **Write path, same as session 1's plan**, plus: `addSociety` and `updateSocietyById` (new this session) need real inserts/updates against `societies`, and `updateSocietyById` specifically needs to work regardless of which society is "active" in the caller's session, since the owner console edits societies it isn't necessarily "inside."
5. **Theme and module data already round-trips cleanly**: `themeKey` is just a string column, `modules` is already shaped as a JSON object matching the `modules jsonb` column in `supabase/schema.sql`, no transformation needed either direction.
6. **Logo upload**: swap the onboarding wizard's and society-detail page's `FileReader`-to-data-URL logic for a real upload to the `documents`-style Storage bucket pattern already described in `supabase/README.md`, storing the resulting URL in `societies.logo_url` instead of a data-URL.
7. **Receipt numbering**: same atomicity note as session 1, needs to happen inside the same transaction as the payment insert (`UPDATE societies SET receipt_seq = receipt_seq + 1 WHERE id = ... RETURNING receipt_seq`), now scoped per-society rather than global, which the schema already reflects.

Test each table's swap against `docs/TESTING_PLAN.md`'s four critical flows before moving to the next table.

## Exact prompts for the next Claude Code session

Prerequisite before any of these: a Supabase project needs to exist and its URL + publishable key need to be in `.env` (see supabase/README.md §1-3), since this can't be created from inside a coding session. As of this writing, both exist, `schema.sql` has not yet been applied to the live project (that's step 0 below, do it manually in the Supabase SQL editor, it's not something a coding session can do either).

**0. Apply the schema (manual, in the Supabase dashboard, not a coding prompt):**
Open the Supabase project's SQL Editor, paste the entire contents of `supabase/schema.sql`, run it once. Confirm no errors, then confirm RLS is actually enabled on every table (Supabase's table editor shows a lock icon per table when it is).

**0b. Bootstrap your own owner membership (manual, SQL editor, do this before ever trying to log in as owner):**
Nothing privileged exists yet, so nothing can grant it through the app - this one row has to be inserted directly:
```sql
insert into memberships (email, society_id, role, status)
values ('YOUR-REAL-EMAIL@example.com', (select id from societies limit 1), 'owner', 'active');
```
Use whichever society id is convenient, the row's society_id doesn't restrict an 'owner' role's actual access (see `has_role(id, array['owner'])` throughout schema.sql, owner bypasses per-society scoping everywhere). Do this in the SQL editor specifically, not through a REST/API call - `enforce_membership_insert()`'s bootstrap bypass checks `session_user`, which only reads as `postgres`/`service_role` for a direct database connection.

~~**1. Supabase client + email magic link auth:**~~ **Done.** `src/lib/supabase.ts` and `src/lib/auth.ts` already have the real client and the real `signInWithOtp` call. `Login.tsx` already calls it when Supabase is configured. Nothing left to build here.

~~**2. The membership-claim step:**~~ **Done.** `src/pages/AuthCallback.tsx` (the `/auth/callback` route the magic link redirect already pointed at) now waits for the real authenticated user, calls `claimMemberships`, and resolves to the right dashboard, a society picker, or `/no-access`. Genuinely unverified against a live project though, see the note at the top of AuthCallback.tsx - this sandbox has no network access to supabase.co. First real test of this needs to happen with a real project, ideally right after 0 and 0b above.

**2b. Resident self-enrollment, real-Supabase version, still needed:**
> The local demo layer (selfEnrollResident in src/lib/store.tsx) and the real database (the enforce_membership_insert trigger + find_flat_for_join function in supabase/schema.sql) both already have the real, tested matching logic - verified by actually running it against a local Postgres 16 instance, see session 8's summary above for the exact scenarios checked. What's missing is the JS function that calls the real Supabase insert for this specific flow from src/pages/Join.tsx: look up the society via a plain select on societies by join_code (public read, no auth needed), call find_flat_for_join(society_id, flat_number) to resolve the flat id, then insert into memberships with that flat_id and whatever the visitor typed - let the trigger decide active vs pending, don't try to replicate that decision in JS for the real path (only the local demo path should do its own phone comparison, since there's no real security boundary to enforce there).

**3. Swap read path, one table group at a time:**
> In src/lib/store.tsx, replace the localStorage-backed `flats` slice with a Supabase query scoped by RLS to the active society. Keep the returned shape matching the Flat type in src/lib/types.ts exactly, including the new email/tenantEmail fields. Don't touch any other table yet. After this one table, STOP and manually verify: log in as two different real memberships (create a second test society first) and confirm neither can see the other's flats. Only continue to the next table once that's confirmed.

*(repeat step 3, swapping one table for the next, in the order listed in schema.sql, verifying isolation after each one, not just at the end)*

**4. Swap the write actions, including the owner-console ones:**
> In src/lib/store.tsx, replace the body of every write action (recordPayment through updateSocietyById, including addSociety) with Supabase insert/update calls against supabase/schema.sql. Keep every function's signature identical to the current Store interface. For vote(), rely on the UNIQUE(poll_id, flat_id) constraint on poll_votes and catch the conflict error rather than pre-checking in JS. For recordPayment's receipt numbering, do the receipt_seq increment atomically in the same transaction as the payment insert.

**5. Logo upload to real storage:**
> Wire real file upload to Supabase Storage for the logo step in src/pages/owner/Onboarding.tsx and src/pages/owner/SocietyDetail.tsx, replacing the current FileReader-to-data-URL logic. Store the resulting public or signed URL in societies.logo_url.

**6. Deploy to Cloudflare Pages:**
> Set up a Cloudflare Pages project connected to this repo's git remote, build command `npm run build`, output directory `dist`. Add VITE_SUPABASE_URL and VITE_SUPABASE_PUBLISHABLE_KEY as Cloudflare Pages environment variables (not committed to the repo, and note the VITE_ prefix specifically, a Next.js-style NEXT_PUBLIC_ prefix will not work here). Confirm the deployed build boots correctly against the live Supabase project.

**7. Payments (Razorpay), once there's real usage to justify it:**
> Add Razorpay UPI-first checkout to src/pages/resident/Bill.tsx, as an additional option alongside the existing manual UPI/bank instructions and "I have paid" pending-confirmation flow, not a replacement for it, some societies may prefer to keep paying manually. Payment verification should happen server-side via a webhook (a Supabase Edge Function) that calls the same recordPayment logic after verifying Razorpay's signature. Never trust a client-side "payment succeeded" callback alone.
